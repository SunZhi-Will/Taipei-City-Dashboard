# 側欄全層級上下間距擴大 / Increase Vertical Spacing Across Sidebar Levels

## 2026-04-22 10:44

- objective:
  - 針對使用者反饋「每個項目上下間距太近」，統一提升桌機與手機側欄項目的垂直間距。

- files:
  - Taipei-City-Dashboard-FE/src/components/utilities/miscellaneous/SideBarTab.vue
  - Taipei-City-Dashboard-FE/src/components/utilities/bars/SideBar.vue
  - Taipei-City-Dashboard-FE/src/components/dialogs/MobileNavigation.vue

- summary:
  - 將 `SideBarTab` 基礎列高由 `22px` 提升為 `26px`，並加入 `margin: 2px 0`，影響第二與第三層項目。
  - 維持第二、第三層縮排一致（皆為 `padding-left: 24px`）。
  - 調整桌機側欄 `h1/h2` 列高為 `26px` 並增加區塊間距；空狀態列高度與行高同步提升為 `26px`。
  - 調整手機側欄 `h1/h2` 最小高度為 `26px`，並增加 `h2` 上下外距。

- change-type:
  - Changed

- technical-details:
  - 以元件級 SCSS 直接覆寫列高與外距，避免改動資料流與互動邏輯。
  - 將 spacing 調整集中在 row-level 樣式（`height/min-height/margin`），保持 icon 與文字在 flex 容器中的垂直置中。

- verification:
  - 針對以下檔案執行診斷：
    - `Taipei-City-Dashboard-FE/src/components/utilities/miscellaneous/SideBarTab.vue`
    - `Taipei-City-Dashboard-FE/src/components/utilities/bars/SideBar.vue`
    - `Taipei-City-Dashboard-FE/src/components/dialogs/MobileNavigation.vue`
  - 結果：No errors found（3/3）。

- performance-impact:
  - 純 CSS 版面調整，未引入額外計算與資源請求，效能影響可忽略。

- impact-risk:
  - 單屏可見項目數降低，捲動出現時機可能提前。
  - 若內容文字較長，行高改變可能讓視覺密度降低但可讀性提升。

- regression-test:
  - 展開 private/public 各區塊，檢查 h1/h2/sidebartab 垂直節奏是否一致。
  - 驗證 hover/active 樣式與 icon 對齊未偏移。
  - 驗證 mobile navigation 展開/收合與列對齊正常。

- traceability:
  - Related logs:
    - user-added/log/2026-04-22/1042-側欄第三層縮排對齊與上下間距增強.md

- next-actions:
  - 若仍需更鬆間距，可將列高再提升至 `28px` 並把 `margin` 調整為 `3px 0`。
