# 側欄第三層縮排對齊與上下間距增強 / Align Level-3 Indent and Increase Vertical Spacing

## 2026-04-22 10:42

- objective:
  - 將第三層項目縮排調整為與第二層一致，並增加第三層上下間距，提升可辨識度。

- files:
  - Taipei-City-Dashboard-FE/src/components/utilities/miscellaneous/SideBarTab.vue

- summary:
  - 將 `.sidebartab-level-3` 的 `padding-left` 從 `36px` 改為 `24px`，與第二層一致。
  - 新增第三層 `height: 26px` 與 `margin: 2px 0`，放大上下間距，讓視覺差異更明顯。

- change-type:
  - Changed

- technical-details:
  - 透過限定在 `.sidebartab-level-3` 的樣式覆寫，避免影響第二層與其他通用項目。
  - 以列高與外距共同調整，確保在既有 flex 對齊下仍維持文字與 icon 的垂直置中。

- verification:
  - 執行檔案診斷檢查：`get_errors` on `Taipei-City-Dashboard-FE/src/components/utilities/miscellaneous/SideBarTab.vue`。
  - 結果：No errors found。

- performance-impact:
  - 僅 CSS 微調，無額外運算與網路成本；對渲染效能影響可忽略。

- impact-risk:
  - 第三層項目高度增加後，同畫面可見項目數略減。
  - 在側欄高度受限情況下，可能更早出現捲動條。

- regression-test:
  - 展開 private/public 各分組，確認第二層與第三層縮排一致。
  - 驗證第三層 hover/active 狀態與 icon 對齊未退化。
  - 驗證側欄收合模式下（`sidebar-collapse`）顯示行為正常。

- traceability:
  - Related logs:
    - user-added/log/2026-04-22/1000-側欄第三層縮排再下修.md

- next-actions:
  - 若使用者仍覺得間距不足，可再微調為 `height: 28px` 或 `margin: 3px 0`。
