# 側欄層級箭頭 icon 英文化回退修正 / Sidebar Chevron Icon Fallback-to-Text Fix

## 2026-04-21 16:43

- objective:
  - 修正第一層與第二層展開箭頭 icon 顯示成英文文字（arrow_drop_up/down）問題。
  - 統一桌機與手機側欄的 chevron icon ligature 渲染規則，避免再回退。

- files:
  - Taipei-City-Dashboard-FE/src/components/utilities/bars/SideBar.vue
  - Taipei-City-Dashboard-FE/src/components/dialogs/MobileNavigation.vue

- summary:
  - 桌機側欄：將所有第一層/第二層 chevron span 補上 material-icons-round class，並套用共用 chevron 字型規則。
  - 手機導覽：同樣補上 material-icons-round class，並補齊 ligature 需要的字型屬性。
  - 補上 text-transform: none + font-feature-settings: "liga"，防止字串被當普通英文文字呈現。

- change-type:
  - Fixed

- technical-details:
  - root cause:
    - chevron 的 icon 樣式先前只在部分層級生效，且未完整設置 ligature/transform 相關屬性，導致 icon 名稱回退為文字。
  - SideBar.vue:
    - 所有 chevron 改為 `<span class="sidebar-chevron material-icons-round" ...>`。
    - 抽出共用 `.sidebar-chevron` 規則並補齊 icon 字型渲染屬性（包含 liga、text-transform: none）。
  - MobileNavigation.vue:
    - chevron 改為 `<span class="mobilenavigation-chevron material-icons-round" ...>`。
    - 補齊 `.mobilenavigation-chevron` 的 ligature 安全樣式。

- verification:
  - get_errors 檢查結果：
    - Taipei-City-Dashboard-FE/src/components/utilities/bars/SideBar.vue -> No errors found
    - Taipei-City-Dashboard-FE/src/components/dialogs/MobileNavigation.vue -> No errors found

- performance-impact:
  - 無顯著效能影響，僅 icon 字型樣式與 class 調整。

- impact-risk:
  - 低風險；僅影響側欄箭頭 icon 顯示，不涉及資料邏輯與 API。

- regression-test:
  - 點擊第一層與第二層節點，確認箭頭顯示為 icon（非英文文字）。
  - 在桌機與手機導覽都驗證展開/收合箭頭切換正常。

- traceability:
  - related-request: 使用者回報第一層與第二層 icon 變英文（2026-04-21）

- next-actions:
  - 如需，我可以再把第一層/第二層改成統一「右箭頭/下箭頭」視覺，層級感會更直觀。
